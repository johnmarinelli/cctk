<% provide(:title, @sketch.title) %>
<%= render partial: 'sketch', 
           locals: { js_snippet: @sketch.snippets.find_by(language: :javascript), 
                     html_snippet: @sketch.snippets.find_by(language: :html)} %>
